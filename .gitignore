/logs
*.log
# Runtime caches — never commit; they may contain API payloads with sensitive identifiers.
/cache/voice_ai_reports/
/cache/voice_ai_insights/
/cache/rate_limit/
/cache/**/*.json

# Secretos reales — NUNCA versionar. Usar config/secrets.example.php como plantilla.
/config/secrets.php

# Backups de trabajo (originales + snapshots) — no versionar
/_design_backup_2026-06-18/
